name: CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
          
    # 'npm install' will be executed with 'jest-action'
    - name: Run Jest 
      uses: stefanoeb/jest-action@1.0.3
      
