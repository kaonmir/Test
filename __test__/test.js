const index = require("../index");

test("should I", () => {
  expect(index()).toBe(1);
});

test("should He ", () => {
  expect(index()).toBe(1);
});

test("should She", () => {
  expect(index()).toBe(3);
});
